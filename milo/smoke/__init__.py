"""milo.smoke — 5-task smoke pack manifest (spec §10.2, plan §9.2).

The manifest itself lives next to this file as ``manifest.yaml``. The
runner is in :mod:`milo.replay.smoke_pack`; this package only exists so
the manifest has a stable import-resolvable home.
"""
